$('.flashes').html('<% flash_messages.each do |type, message| %><%= content_tag :div, message, class: "flash flash_#{type}" %><% end %>');
alert('<% flash_messages.each do |type, message| %><%= type == "notice" ? "Updated" : "NOT Updated" %><% end %>');
$('#videoVideoLinks .panel_contents').html('<%= escape_javascript(render 'video_video_links', {video_link: @video_link}) %>');
